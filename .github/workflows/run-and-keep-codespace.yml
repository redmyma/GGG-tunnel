name: Auto Run GG-Tunnel and Save Config

on:
  workflow_dispatch:

jobs:
  auto-run-codespace:
    name: Auto Run GG-Tunnel in Codespace
    runs-on: ubuntu-latest
    permissions:
      contents: write
      actions: write
    env:
      GH_TOKEN: ${{ secrets.PAT_TOKEN }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install GitHub CLI
        run: |
          type -p curl >/dev/null || (sudo apt update && sudo apt install curl -y)
          curl -fsSL https://cli.github.com/packages/githubcli-archive-keyring.gpg | sudo dd of=/usr/share/keyrings/githubcli-archive-keyring.gpg
          sudo chmod go+r /usr/share/keyrings/githubcli-archive-keyring.gpg
          echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/githubcli-archive-keyring.gpg] https://cli.github.com/packages stable main" | sudo tee /etc/apt/sources.list.d/github-cli.list > /dev/null
          sudo apt update
          sudo apt install gh -y

      - name: Auth with GitHub CLI
        run: |
          if [ -n "${{ secrets.PAT_TOKEN }}" ]; then
            echo "${{ secrets.PAT_TOKEN }}" | gh auth login --with-token
          else
            echo "PAT_TOKEN is missing! Starting Web Login Flow..."
            timeout 300 gh auth login --web --scopes "repo,codespace,workflow"
          fi
          gh auth status

      - name: Create codespace
        id: create_cs
        run: |
          echo "Creating codespace..."
          
          RAW_OUTPUT=$(gh codespace create \
            --repo ${{ github.repository }} \
            --branch main \
            --machine basicLinux32gb \
            --display-name "gg-tunnel-run-$(date +%Y%m%d-%H%M%S)" \
            --idle-timeout 30m \
            --retention-period 24h)
          
          echo "Raw output: $RAW_OUTPUT"
          
          CS_NAME=$(echo "$RAW_OUTPUT" | grep -o '[a-zA-Z0-9-]\+-[a-zA-Z0-9]\+-[a-zA-Z0-9]\+-[a-zA-Z0-9]\+' | head -n 1)
          if [ -z "$CS_NAME" ]; then
            CS_NAME=$(echo "$RAW_OUTPUT" | tail -n 1)
          fi
          
          echo "cs_name=$CS_NAME" >> $GITHUB_OUTPUT

      - name: Settle down environment
        run: |
          echo "Waiting 60 seconds for container startup..."
          sleep 60

      - name: Public 443 Port
        run: |
          CS="${{ steps.create_cs.outputs.cs_name }}"
          echo "Setting port 443 to public..."
          gh codespace ports visibility 443:public -c "$CS" || true

      - name: Capture Config Output
        run: |
          CS="${{ steps.create_cs.outputs.cs_name }}"
          echo "Connecting via SSH and executing print script..."
          
          gh codespace ssh -c "$CS" -- "/usr/local/bin/print-configs.sh" > terminal_full_output.log 2>&1 || true
          
          echo "=== CAPTURED LOG PREVIEW ==="
          cat terminal_full_output.log

      - name: Extract config
        run: |
          if grep -q "G-Tunnel GAMING CONFIGS" terminal_full_output.log; then
            sed -n '/G-Tunnel GAMING CONFIGS/,$p' terminal_full_output.log > Config.txt
          else
            cat terminal_full_output.log > Config.txt
          fi

      - name: Save Config.txt
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add Config.txt
          git commit -m "Update Config.txt [skip ci]" || true
          git push origin main || true

      - name: Cleanup Codespace
        if: always()
        run: |
          CS="${{ steps.create_cs.outputs.cs_name }}"
          if [ -n "$CS" ]; then
            echo "Deleting codespace: $CS"
            gh codespace delete -c "$CS" --force || true
          fi
